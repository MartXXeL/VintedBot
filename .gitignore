# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Entorno
.env
.envrc
.direnv/

# IDEs
.vscode/
.idea/
.claude/

# Datos en tiempo de ejecución (BD, fotos subidas, sesiones de Vinted)
data/

# Sesiones/cookies: credenciales vivas, nunca al repositorio
*.session
cookies.txt
session_*.json

# Logs y artefactos manuales
*.log

# Imágenes de referencia (recortes de prensa, mockups de terceros usados solo
# como inspiración visual): no son propias del proyecto ni del autor, así que
# se guardan localmente para consultarlas pero no se versionan. Lo que sí es
# del proyecto está en src/ui/static/img/ y en el resto de docs/assets/.
/docs/assets/reference/
